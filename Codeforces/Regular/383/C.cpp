#include<cstdio>
#include<cmath>
#include<iostream>
#include<cstdlib>
#include<cstring>
#include<algorithm>
#define MAXN 100
#define INF 10000
using namespace std;
int n,num[MAXN],f[MAXN],ans;
int main()
{
    memset(f,sizeof(f),0);
    scanf("%d",&n);
    for(int i=0;i<n;i++)
        scanf("%d",&num[i]);
    int k=0;
    ans=INF;
    while(k<n)
    {
        if(!f[k])
        {

        }
    }
}
