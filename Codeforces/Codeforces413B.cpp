#include<cstdio>
#include<cmath>
#include<iostream>
#include<cstdlib>
#include<cstring>
#include<algorithm>
#include<queue>
#define MAXN 200000
using namespace std;
int p[MAXN],a[MAXN],b[MAXN],n,m;
int main()
{
    priority_queue<int> que1,que2,que3;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&p[i]);
    }
     for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
     for(int i=0;i<n;i++)
    {
        scanf("%d",&b[i]);
    }
}
