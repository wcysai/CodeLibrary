/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
    int tot,sum[100005],maxi[100005],l[100005],r[100005],val[100005];
    void dfs(TreeNode* t)
    {
        if(t==null) return;
        tot++;
        val[]
    }
public:
    int getMaxLayerSum(TreeNode* root) {
    
    }
};
